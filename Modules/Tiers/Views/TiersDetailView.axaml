<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:GestionCommerciale.Modules.Tiers.ViewModels"
             x:Class="GestionCommerciale.Modules.Tiers.Views.TiersDetailView"
             x:DataType="vm:TiersDetailViewModel">
  <Grid ColumnDefinitions="260,*" Margin="0,0,0,8">
    <ScrollViewer Grid.Column="0">
      <StackPanel Spacing="8">
        <Button Content="{Binding BtnBackList}" Command="{Binding BackCommand}" HorizontalAlignment="Left"/>
        <ComboBox ItemsSource="{Binding Types}"
                  SelectedItem="{Binding Type}"
                  IsEnabled="{Binding CanEditType}"
                  IsVisible="{Binding CanEditType}">
          <ComboBox.ItemTemplate>
            <DataTemplate>
              <TextBlock Text="{Binding ., Converter={StaticResource TypeTiersLabelConverter}}"/>
            </DataTemplate>
          </ComboBox.ItemTemplate>
        </ComboBox>
        <Border IsVisible="{Binding !CanEditType}"
                Padding="10,8"
                Background="{DynamicResource CourseraSurfaceMutedBrush}"
                BorderBrush="{DynamicResource CourseraBorderBrush}"
                BorderThickness="1"
                CornerRadius="6">
          <TextBlock Text="{Binding TypeLabel}" Opacity="0.85"/>
        </Border>

        <StackPanel Spacing="3">
          <TextBlock Text="{Binding WmNom}"/>
          <TextBox Watermark="{Binding WmNom}" Text="{Binding Nom}"/>
        </StackPanel>

        <StackPanel Spacing="3">
          <TextBlock Text="{Binding WmIce}"/>
          <TextBox Watermark="{Binding WmIce}" Text="{Binding Ice}"/>
        </StackPanel>

        <StackPanel Spacing="3">
          <TextBlock Text="{Binding WmAdresse}"/>
          <TextBox Watermark="{Binding WmAdresse}" Text="{Binding Adresse}"/>
        </StackPanel>

        <StackPanel Spacing="3">
          <TextBlock Text="{Binding WmVille}"/>
          <TextBox Watermark="{Binding WmVille}" Text="{Binding Ville}"/>
        </StackPanel>

        <StackPanel Spacing="3">
          <TextBlock Text="{Binding WmTelephone}"/>
          <TextBox Watermark="{Binding WmTelephone}" Text="{Binding Telephone}"/>
        </StackPanel>

        <StackPanel Spacing="3">
          <TextBlock Text="{Binding WmEmail}"/>
          <TextBox Watermark="{Binding WmEmail}" Text="{Binding Email}"/>
        </StackPanel>

        <StackPanel Spacing="3">
          <TextBlock Text="{Binding WmConditions}"/>
          <TextBox Watermark="{Binding WmConditions}" Text="{Binding ConditionsPaiement}"/>
        </StackPanel>

        <CheckBox Content="{Binding ChkActif}" IsChecked="{Binding Actif}"/>
        <Button Content="{Binding BtnSave}" Command="{Binding SaveCommand}"/>
      </StackPanel>
    </ScrollViewer>

    <DockPanel Grid.Column="1" Margin="16,0,0,0" IsVisible="{Binding ShowLedger}">
      <StackPanel DockPanel.Dock="Top" Spacing="8" Margin="0,0,0,8">
        <TextBlock Text="{Binding LblLedgerTitle}" FontWeight="SemiBold" FontSize="16"/>
        <Grid ColumnDefinitions="*,Auto">
          <Border Grid.Column="0"
                  Padding="8,5"
                  Background="#DBEAFE"
                  BorderBrush="#3B82F6"
                  BorderThickness="1"
                  CornerRadius="6"
                  HorizontalAlignment="Left"
                  VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal" Spacing="6">
              <TextBlock Text="{Binding LblSoldeActuel}" FontWeight="SemiBold" FontSize="12" Foreground="#1E40AF" VerticalAlignment="Center"/>
              <TextBlock Text="{Binding SoldeActuelText}" FontWeight="SemiBold" FontSize="13" Foreground="#1E40AF" VerticalAlignment="Center"/>
            </StackPanel>
          </Border>
          <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
            <Border Padding="8,5"
                    Background="#ECFDF5"
                    BorderBrush="#10B981"
                    BorderThickness="1"
                    CornerRadius="6"
                    VerticalAlignment="Center">
              <StackPanel Orientation="Horizontal" Spacing="6">
                <TextBlock Text="{Binding LblLedgerPeriod}" FontWeight="SemiBold" FontSize="12" Foreground="#065F46" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding PeriodSoldeText}" FontWeight="SemiBold" FontSize="13" Foreground="#065F46" VerticalAlignment="Center"/>
              </StackPanel>
            </Border>
            <Button Content="{Binding BtnPdfLedger}" Command="{Binding ExportLedgerPdfCommand}" VerticalAlignment="Center"/>
          </StackPanel>
        </Grid>

        <WrapPanel Orientation="Horizontal">
          <WrapPanel.Styles>
            <Style Selector="Button.range">
              <Setter Property="Padding" Value="10,4"/>
              <Setter Property="MinHeight" Value="32"/>
              <Setter Property="Margin" Value="0,0,6,6"/>
              <Setter Property="VerticalAlignment" Value="Bottom"/>
              <Setter Property="Background" Value="Transparent"/>
              <Setter Property="BorderBrush" Value="{DynamicResource CourseraBorderBrush}"/>
              <Setter Property="BorderThickness" Value="1"/>
              <Setter Property="CornerRadius" Value="6"/>
            </Style>
            <Style Selector="Button.range.active">
              <Setter Property="Background" Value="#DBEAFE"/>
              <Setter Property="BorderBrush" Value="#3B82F6"/>
              <Setter Property="Foreground" Value="#1E40AF"/>
              <Setter Property="FontWeight" Value="SemiBold"/>
            </Style>
          </WrapPanel.Styles>
          <Button Classes.range="True" Classes.active="{Binding IsLedgerRangeToday}" Content="{Binding BtnLedgerToday}" Command="{Binding SetLedgerTodayCommand}"/>
          <Button Classes.range="True" Classes.active="{Binding IsLedgerRangeWeek}" Content="{Binding BtnLedgerWeek}" Command="{Binding SetLedgerWeekCommand}"/>
          <Button Classes.range="True" Classes.active="{Binding IsLedgerRangeMonth}" Content="{Binding BtnLedgerMonth}" Command="{Binding SetLedgerMonthCommand}"/>
          <Button Classes.range="True" Classes.active="{Binding IsLedgerRangeYear}" Content="{Binding BtnLedgerYear}" Command="{Binding SetLedgerYearCommand}"/>
          <Button Classes.range="True" Classes.active="{Binding IsLedgerRangeAll}" Content="{Binding BtnLedgerAll}" Command="{Binding SetLedgerAllCommand}"/>
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="8,0,0,6" VerticalAlignment="Bottom" Spacing="8">
            <StackPanel Spacing="2" Width="150">
              <TextBlock Text="{Binding LblLedgerFrom}" Opacity="0.7" FontSize="11"/>
              <CalendarDatePicker SelectedDate="{Binding LedgerFrom, Mode=TwoWay}" MinHeight="32" HorizontalAlignment="Stretch"/>
            </StackPanel>
            <StackPanel Spacing="2" Width="150">
              <TextBlock Text="{Binding LblLedgerTo}" Opacity="0.7" FontSize="11"/>
              <CalendarDatePicker SelectedDate="{Binding LedgerTo, Mode=TwoWay}" MinHeight="32" HorizontalAlignment="Stretch"/>
            </StackPanel>
          </StackPanel>
        </WrapPanel>

        <Border IsVisible="{Binding ShowBulkPay}"
                Padding="10,8"
                Background="{DynamicResource CourseraSurfaceMutedBrush}"
                BorderBrush="{DynamicResource CourseraBorderBrush}"
                BorderThickness="1"
                CornerRadius="8">
          <StackPanel Spacing="8">
            <TextBlock Text="{Binding LblBulkPayTitle}" FontWeight="SemiBold" FontSize="13"/>
            <Grid ColumnDefinitions="140,140,160,*,Auto"
                  RowDefinitions="Auto,Auto">
              <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding LblBulkPayAmount}" Opacity="0.7" FontSize="11" Margin="0,0,12,3"/>
              <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding LblBulkPayMode}" Opacity="0.7" FontSize="11" Margin="0,0,12,3"/>
              <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding LblBulkPayDate}" Opacity="0.7" FontSize="11" Margin="0,0,12,3"/>
              <TextBlock Grid.Row="0" Grid.Column="3" Text="{Binding WmBulkPayRef}" Opacity="0.7" FontSize="11" Margin="0,0,12,3"/>

              <NumericUpDown Grid.Row="1" Grid.Column="0"
                             Value="{Binding BulkPayAmount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Minimum="0" Increment="1" FormatString="0.00"
                             HorizontalAlignment="Stretch" MinHeight="36" Margin="0,0,12,0"/>
              <ComboBox Grid.Row="1" Grid.Column="1"
                        ItemsSource="{Binding BulkPayModes}" SelectedItem="{Binding BulkPayMode}"
                        HorizontalAlignment="Stretch" MinHeight="36" Margin="0,0,12,0">
                <ComboBox.ItemTemplate>
                  <DataTemplate>
                    <TextBlock Text="{Binding ., Converter={StaticResource ModePaiementLabelConverter}}"/>
                  </DataTemplate>
                </ComboBox.ItemTemplate>
              </ComboBox>
              <CalendarDatePicker Grid.Row="1" Grid.Column="2"
                                 SelectedDate="{Binding BulkPayDate, Mode=TwoWay}"
                                 HorizontalAlignment="Stretch" MinHeight="36" Margin="0,0,12,0"/>
              <TextBox Grid.Row="1" Grid.Column="3"
                       Watermark="{Binding WmBulkPayRef}" Text="{Binding BulkPayReference}"
                       HorizontalAlignment="Stretch" MinHeight="36" Padding="8,5" Margin="0,0,12,0"/>
              <Button Grid.Row="1" Grid.Column="4"
                      Content="{Binding BtnBulkPay}" Command="{Binding BulkPayCommand}"
                      Padding="12,0" MinHeight="36" HorizontalAlignment="Left"
                      VerticalAlignment="Stretch" VerticalContentAlignment="Center"/>
            </Grid>
          </StackPanel>
        </Border>
      </StackPanel>

      <TextBlock DockPanel.Dock="Top" Text="{Binding LblLedgerSaveFirst}" Classes="subtle"
                 IsVisible="{Binding ShowLedgerSaveFirst}" Margin="0,8,0,0"/>
      <TextBlock DockPanel.Dock="Top" Text="{Binding LblLedgerEmpty}" Classes="subtle"
                 IsVisible="{Binding ShowLedgerEmpty}" Margin="0,8,0,0"/>

      <Border Padding="12" Background="{DynamicResource CourseraSurfaceBrush}"
              BorderBrush="{DynamicResource CourseraBorderBrush}" BorderThickness="1" CornerRadius="12">
        <ScrollViewer>
          <StackPanel Spacing="4">
            <Grid ColumnDefinitions="90,2*,1.2*,90,90,100" Margin="0,0,0,4">
              <TextBlock Grid.Column="0" Text="{Binding LblLedgerDate}" FontWeight="SemiBold" FontSize="12"/>
              <TextBlock Grid.Column="1" Text="{Binding LblLedgerDesignation}" FontWeight="SemiBold" FontSize="12"/>
              <TextBlock Grid.Column="2" Text="{Binding LblLedgerObservation}" FontWeight="SemiBold" FontSize="12"/>
              <TextBlock Grid.Column="3" Text="{Binding LblLedgerDebit}" FontWeight="SemiBold" FontSize="12" HorizontalAlignment="Right"/>
              <TextBlock Grid.Column="4" Text="{Binding LblLedgerCredit}" FontWeight="SemiBold" FontSize="12" HorizontalAlignment="Right"/>
              <TextBlock Grid.Column="5" Text="{Binding LblLedgerBalance}" FontWeight="SemiBold" FontSize="12" HorizontalAlignment="Right"/>
            </Grid>

            <ItemsControl ItemsSource="{Binding LedgerRows}">
              <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="vm:ClientLedgerDisplayRow">
                  <Border Padding="8,6" Margin="0,0,0,2"
                          Background="{DynamicResource CourseraSurfaceMutedBrush}" CornerRadius="6">
                    <Grid ColumnDefinitions="90,2*,1.2*,90,90,100">
                      <TextBlock Grid.Column="0" Text="{Binding DateText}" FontSize="12" VerticalAlignment="Center"/>
                      <TextBlock Grid.Column="1" Text="{Binding Designation}" FontSize="12" VerticalAlignment="Center" TextWrapping="Wrap"/>
                      <TextBlock Grid.Column="2" Text="{Binding Observation}" FontSize="12" Classes="subtle" VerticalAlignment="Center" TextWrapping="Wrap"/>
                      <TextBlock Grid.Column="3" Text="{Binding DebitText}" FontSize="12" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                      <TextBlock Grid.Column="4" Text="{Binding CreditText}" FontSize="12" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                      <TextBlock Grid.Column="5" Text="{Binding BalanceText}" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    </Grid>
                  </Border>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </StackPanel>
        </ScrollViewer>
      </Border>
    </DockPanel>
  </Grid>
</UserControl>
